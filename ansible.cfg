[defaults]
nocows = True
roles_path = ./roles
collections_path = ./collections
inventory = inventory.yaml
callback_result_format = yaml
host_key_checking = False
# stdout_callback = yaml    # removed in community.general 12.0; use callback_result_format above
# Bootstrap run from workstation — target disk in USB enclosure
connection = local
become = true
become_user = root
