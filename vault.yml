---
# ── SECRETS (REQUIRED) ──────────────────────────────────────────
# All variables in this file MUST be configured.
# It is HIGHLY RECOMMENDED to encrypt this file using ansible-vault:
#   ansible-vault encrypt vault.yml
#
# bootstrap_luks_password: "CHANGEME_LUKS_PASS"
# bootstrap_user_password: "CHANGEME_USER_PASS"
# bootstrap_ssh_public_key: "ssh-ed25519 AAAA... your-key-here"

bootstrap_luks_password: "CHANGEME_LUKS_PASS"
bootstrap_user_password: "CHANGEME_USER_PASS"
bootstrap_ssh_public_key: "CHANGEME_SSH_PUB_KEY"
